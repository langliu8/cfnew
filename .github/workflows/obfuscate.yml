name: Legacy obfuscation workflow disabled

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  legacy-obfuscate:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the preferred build path
        run: |
          echo "Cloudflare Workers Builds now runs npm run build for automatic deployments."
          echo "This manual workflow is retained only for legacy artifact generation."
